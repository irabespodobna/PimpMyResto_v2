<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8">
    <script type="text/javascript" src="application.js"></script>
    <h1>Bienvenue to <%= @resto.name %>, FDP</h1>
  </head>

  <body>
    There are <%= @num_votes %> votes about <%= @resto.name %> :
      <p> <%= @num_vegan_votes  %> voted for a vegan option! </p>
      <p> <%= @num_veggie_votes  %> voted for a veggie option! </p>
      <p> <%= @num_gluten_votes  %> voted for a gluten option! </p>
      <% @surveys.each do |survey|  %>
      <p>
        <%= survey.resto.name %> -- <%= survey.foodstyle_category.name %>
      </p>
      <% end %>

      <h3>Interested? Vote yourself for a new option at <%= @resto.name %></h3>
      <%= form_tag "/restos/#{params[:id]}", method: 'post' do %>
      <input type="hidden" name="resto_id" value=<%= @resto.id  %>/>

      <select name="foodstyle_category_id">
        <% @foodstyle_categories.each do |category| %>
        <option value="<%= category.id %>"><%= category.name %></option>
        <% end %>
      </select>

      <input type="submit" value="Envoyez votre choix"/>
      <% end %>
    </body>

    <p><a href="/restos">Retourner à l’index</a></p>
