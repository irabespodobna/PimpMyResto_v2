<h1>Bienvenue to <%= @resto.name %></h1>

<p> There are <%= @num_votes %> votes about <%= @resto.name %> :  </p>
<p>
  <%= @num_vegan_votes %>
  voted for a vegan option!
</p>
<p>
  <%= @num_veggie_votes %>
  voted for a veggie option!
</p>
<p>
  <%= @num_gluten_votes %>
  voted for a gluten option!
</p>

<h3>Interested? Vote yourself for a new option at
  <%= @resto.name %></h3>
<%= form_tag "/restos/#{params[:id]}", method: 'post' do %>
<input type="hidden" name="resto_id" value=<%= @resto.id %>/>

<select name="foodstyle_category_id">
  <% @foodstyle_categories.each do |category| %>
    <option value="<%= category.id %>"><%= category.name %></option>
  <% end %>
</select>


<!-- <form action="/users/sign_up"> -->
<button type="submit">Envoyez votre choix</button>
<!--</form>-->
<% end %>

<p>
  <a href="/restos">Retourner à l’index</a>
</p>


<% task_completion = (@num_votes.to_f / 250) * 100 %>
<% task_completion_vegan = (@num_vegan_votes.to_f / 250) * 100 %>
<% task_completion_veggie = (@num_veggie_votes.to_f / 250) * 100 %>
<% task_completion_gluten = (@num_gluten_votes.to_f / 250) * 100 %>



<div class="progress" style="height:120px; margin-left:10%; margin-right:10%;">
    <div class="progress-bar" role="progressbar" style="writing-mode:vertical-rl; text-orientation:sideways; width: <%= task_completion_vegan %>%" aria-valuenow="<%= task_completion_vegan %>" aria-valuemin="0" aria-valuemax="100" > <%= @num_vegan_votes %> Vegans</div>
    <div class="progress-bar bg-success" role="progressbar" style="writing-mode:vertical-rl; text-orientation:sideways; width: <%= task_completion_veggie%>%" aria-valuenow="<%= task_completion_veggie %>" aria-valuemin="0" aria-valuemax="100"><%= @num_veggie_votes %> Veggies</div>
    <div class="progress-bar bg-danger" role="progressbar" style="writing-mode:vertical-rl; text-orientation:sideways; width: <%= task_completion_gluten%>%" aria-valuenow="<%= task_completion_gluten %>" aria-valuemin="0" aria-valuemax="100" ><%= @num_gluten_votes %> Glutens</div>
</div>

<p>Votre vote fait parti des <%= task_completion %>% de gens ayant vote avec vous =]</p>
<p>Au total, il y a <%= @num_votes %>/250 realises a ce jour !</p>
