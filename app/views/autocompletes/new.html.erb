<h1>New Autocomplete</h1>
<div class="flexbox">
  <h1>New Autocomplete</h1>
  <%= render 'form', autocomplete: @autocomplete %>
  <%= link_to 'Back', autocompletes_path %>
</div>

<%= render 'form', autocomplete: @autocomplete %>

<%= link_to 'Back', autocompletes_path %>
